<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiPersianToolkit.Controls.TreeView"
             xmlns:controls="clr-namespace:MauiPersianToolkit.Controls"
             x:Name="treeView">
    <CollectionView x:Name="list"
                    SelectionMode="None"
                    VerticalOptions="Fill"
                    HorizontalOptions="Fill">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="controls:TreeViewItem">
                <controls:TreeViewNode ShowItem="{Binding .}"
                                       Owner="{Binding Source={x:Reference treeView}, x:DataType=controls:TreeView}"
                                       ItemHeight="{Binding ItemHeight, Source={x:Reference treeView}, x:DataType=controls:TreeView}"
                                       ItemTemplate="{Binding ItemTemplate, Source={x:Reference treeView}, x:DataType=controls:TreeView}"/>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentView>
